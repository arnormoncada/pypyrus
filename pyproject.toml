[project]
name = "pypyrus"
version = "0.1.0"
description = "Data provenance layer for reproducible ML training"
requires-python = ">=3.9"

dependencies = [
    "torch",
    "torchvision"
]

[tool.setuptools.packages.find]
include = ["pypyrus*"]